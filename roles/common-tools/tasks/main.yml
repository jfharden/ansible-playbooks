---
- name: "Install Common Tools"
  become: true
  ansible.builtin.apt:
    name: "{{ item }}"
    state: present
  loop:
    - 7zip
    - pwgen
    - rename
    - silversearcher-ag
    - tree
    - unrar
